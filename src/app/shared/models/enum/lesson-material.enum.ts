export enum LessonMaterialStatus {
  Draft = 0,
  Pending = 1,
  Approved = 2,
  Rejected = 3,
}

export enum LessonMaterialVisibility {
  Private = 0,
  School = 1,
}

export enum ContentType {
  Video = 0,
  Audio = 1,
  DOCX = 2,
  PDF = 3,
}
