import { ReadonlyModeDirective } from './readonly-mode.directive';

describe('ReadonlyModeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyModeDirective();
    expect(directive).toBeTruthy();
  });
});
