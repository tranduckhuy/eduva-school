import { RouteMetadataDirective } from './route-metadata.directive';

describe('RouteMetadataDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteMetadataDirective();
    expect(directive).toBeTruthy();
  });
});
